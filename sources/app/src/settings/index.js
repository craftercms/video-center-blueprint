
  const studioConfig = {
    baseUrl: "http://localhost:8080",
    site: "video-center",
    navTreeBase: "/site/website"
  }

  const themeConfig = {
    theme: 'themedefault'
  };

  export {
    studioConfig,
    themeConfig
  };
  